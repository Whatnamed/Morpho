A 关键结论 object: one retained idea, read against a vertical marker column.

\`\`\`jsx
<KeyConclusionObject title="连续支持比单点扶手更符合真实动作路径。" detail="真实动作是连续的，扶手却是离散的。" />
<KeyConclusionObject marker="待验证" role="待验证" title="租住场景可接受的安装方式。" />
\`\`\`

When the conclusion was extracted from a research object, set \`marker\` and \`role\` to the extraction kind so the user can see where it came from.
