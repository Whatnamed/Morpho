A draft an Operation produced, placed on the canvas awaiting confirmation — marked by a folded corner and a 待确认 pill.

\`\`\`jsx
<ProposalDraftObject title="夜间起身路径与扶持需求的前期调研" summary="真实动作是连续的，现有产品是离散的。" />
\`\`\`

A draft is a candidate, never a decision: the AI cannot accept, promote or discard it. Confirming turns it into a \`ResearchObject\`, \`DefinitionObject\` or \`KeyConclusionObject\`.
