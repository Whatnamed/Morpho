Holds the slot on the canvas while an image is generating — a warm plate with a slow shimmer and a few drifting grains.

\`\`\`jsx
<PendingImageObject title="柔光轨道 v3 · 转角一体" width={320} height={214} />
\`\`\`

It occupies the position the finished image will take, so results never displace the source image. Both animations stop under \`prefers-reduced-motion\`.
