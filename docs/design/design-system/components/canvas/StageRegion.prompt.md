A stage landmark: a soft tinted wash with a small title, marking where a phase of the project lives.

\`\`\`jsx
<StageRegion left={120} top={120} width={640} height={540} colorKey="warmSand" title="资料与研究" />
<StageRegion left={1600} top={760} width={560} height={460} colorKey="sage" title="交付整理" locked />
\`\`\`

Default pairings: 资料与研究 warmSand · 设计定义 mistBlue · 方向与视觉 violetGray · 交付整理 sage. Fill sits at 16% so it reads as a zone, not a card. Never a dashed flowchart box, and never force the objects inside into a grid.
