# Morpho v3.2：AI 连续性与生成链路收口说明

这套文件是在现有 v3.1 基线之上完成的 AI 子系统规则收口，是连续会话、项目记忆、阶段记录、Agent 和图像生成链路的唯一当前依据。v3.1 中未被本轮明确替换的画布、对象、状态、交付与归档规则继续有效。

## 当前产品文档组状态

产品规则的整体当前基线是 **Morpho v3.2**。下表中的“文件版本”表示该文件最后一次发生实质性规则更新，
不表示文件是否仍有效；当前有效性由本索引和 00 号完整性账本共同决定。02 与 05 虽然文件版本较早，
但没有被 v3.2 的 AI 连续性更新替代，仍是当前产品规则的一部分。

| 文件 | 当前有效性 | 主要职责 | 文件最后规则版本 | 属于当前 v3.2 基线 | 被哪些后续规则覆盖 |
|---|---|---|---|---|---|
| `README_本次更新说明.md` | 当前索引与更新说明 | 说明本轮 v3.2 变更、阅读顺序和文档状态 | v3.2 | 是 | 由 00 号账本维护跨文档继承关系 |
| `00_Morpho_v3_规则继承、覆盖与完整性账本.md` | 当前权威控制文档 | 记录继承、替换、待定项和完整性校验 | v3.2 | 是 | 无；它约束其余产品正文的解释方式 |
| `01_Morpho_产品定义与总体流程.md` | 当前有效 | 产品定位、用户、总体流程和产品边界 | v3.2 | 是 | 03/04 补充 AI 连续性与项目记录；02/05 负责各自细则 |
| `02_Morpho_工作台、画布与对象规则.md` | 当前有效 | 工作台、画布、对象、选择、关系和交互边界 | v3 | 是 | 未被 v3.2 AI 连续性更新替代 |
| `03_Morpho_AI工作流程、阶段Context与连续性机制.md` | 当前有效 | 连续 Agent、任务策略、Context、压缩和 AI 行为 | v3.2 | 是 | 替代早期按阶段/lane 隔离聊天的规则 |
| `04_Morpho_状态、版本、项目记录与记忆.md` | 当前有效 | 状态、版本、Memory Kernel、阶段记录和受控写入 | v3.2 | 是 | 替代早期 Markdown 事实源和事件即阶段记录的理解 |
| `05_Morpho_项目入口、资产、搜索、导入与归档.md` | 当前有效 | 项目入口、资产、搜索、导入、输出和归档/备份 | v3.1 | 是 | 未被 v3.2 AI 连续性更新替代 |

技术架构、运行方式、发布阶段和历史迁移不属于这组产品正文，分别以 [`docs/architecture/README.md`](../architecture/README.md)
和 [`docs/operations/runbook.md`](../operations/runbook.md) 为入口。

## 本次正式写入的变化

1. **一个连续 Agent 会话**：未压缩历史在有效 Token Budget 内完整携带；阶段、当前重点、方向、选择对象和 VisualBranch 只影响任务策略与资料选择，不再切断聊天历史。
2. **Token 驱动自动压缩**：接近 Provider 有效上下文上限时才压缩；系统保存摘要版本与覆盖边界，后续携带摘要和边界后的完整消息，原始聊天永不删除。
3. **来源驱动的项目记忆**：七类项目记忆保留为当前有效、可版本化、可追溯的阅读投影；结构化对象、修订、决定、操作与交付状态仍是权威事实源，Markdown 只用于阅读和导出。
4. **当前有效阶段记录**：六类阶段记录保存各阶段目前成立的结论、依据、风险与下一步；Continuity Events 保存历史，两者不再互相冒充，也不形成独立阶段聊天。
5. **统一 Agent Runtime**：正式 AI 面板只使用 `/api/ai/agent/turns/**` A+ 资源、Responses 流式协议、统一 Context Builder、Prompt Registry、Memory Kernel 与确认策略；独立 `/api/ai/chat` 不服务正式面板。
6. **统一图像计划与 Prompt 编译**：Agent 输出结构化视觉意图，Morpho 确定性解析参考图并按图片角色、方向、设计定义、项目偏好和目标模型编译最终 Provider Prompt。
7. **数量快捷项不是能力上限**：`1 / 2 / 4 / 6` 只保留为 UI 快捷项；用户明确要求 `3 / 5 / 9 / 12` 或四个以上方向时允许透明分批执行，仅受真实 Provider、配额、并发和防失控约束。
8. **后台记录可读但无需手工管理**：项目记录区默认展示当前项目记忆和已发生阶段记录，并可追溯来源和历史修订；用户仍通过真实对象、明确状态和自然语言决定项目事实。
9. **固定 Morpho 内部 Context 策略**：唯一生产策略为 `256000 / 204800 / 230400 / 16000`（window / prepare / compact / target）；80% 只准备，90% 才持久化压缩，不随 Provider 宣称窗口变化。
10. **默认记忆与历史分层**：项目概览、设计定义、稳定偏好、待确认问题和当前相关阶段记录以有界紧凑内容默认进入 Agent；完整历史、revision、来源和被替代决定仍必须按需真实读取。
11. **受控写入与状态分类**：长期记忆只接受用户明确或已确认来源；UI-only 操作消息不进入模型 Context；用户明确参考图可跨方向保留；DecisionRecord 区分当前、已替代、历史和待复核。

## 本次没有进入的内容

- 更换当前文本或图像 Provider、框架、数据库或核心依赖；
- 起始阶段“自动读取刚导入的一批资料”的增强能力；
- 最终 PPTX、HTML PPT、自动展板排版、Figma 级编辑；
- 更复杂协作、外部网页调研、资产治理和跨项目检索。
